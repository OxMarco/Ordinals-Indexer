version: '3.8'
services:
  app:
    env_file:
      - .env
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
  #   depends_on:
  #     - mongodb
  #     - redis

  # mongodb:
  #   image: mongo:latest
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongodb_data:/data/db

  # redis:
  #   image: redis:latest
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

# volumes:
#   mongodb_data:
#     driver: local
#   redis_data:
#     driver: local
